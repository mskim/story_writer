<div class="relative inline-block text-left" data-controller="dropdown">
  <button type="button" 
          class="inline-flex items-center gap-2 px-3 py-2 text-sm font-medium text-gray-700 bg-white border border-gray-300 rounded-lg hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500"
          data-action="click->dropdown#toggle click@window->dropdown#hide"
          aria-expanded="false">
    <% if I18n.locale == :ko %>
      <span class="text-lg">🇰🇷</span>
      <span>한국어</span>
    <% else %>
      <span class="text-lg">🇺🇸</span>
      <span>English</span>
    <% end %>
    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
    </svg>
  </button>

  <div data-dropdown-target="menu"
       class="hidden absolute right-0 z-10 mt-2 w-48 origin-top-right rounded-lg bg-white shadow-lg ring-1 ring-black ring-opacity-5 focus:outline-none">
    <div class="py-1">
      <%= form_with url: update_language_path, method: :patch, data: { turbo: false }, class: "w-full" do |f| %>
        <%= f.hidden_field :language, value: 'en' %>
        <button type="submit" class="flex items-center gap-3 w-full px-4 py-2 text-sm text-gray-700 hover:bg-gray-100 text-left <%= I18n.locale == :en ? 'bg-indigo-50' : '' %>">
          <span class="text-lg">🇺🇸</span>
          <span>English</span>
          <% if I18n.locale == :en %>
            <svg class="ml-auto w-4 h-4 text-indigo-600" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"></path>
            </svg>
          <% end %>
        </button>
      <% end %>
      
      <%= form_with url: update_language_path, method: :patch, data: { turbo: false }, class: "w-full" do |f| %>
        <%= f.hidden_field :language, value: 'ko' %>
        <button type="submit" class="flex items-center gap-3 w-full px-4 py-2 text-sm text-gray-700 hover:bg-gray-100 text-left <%= I18n.locale == :ko ? 'bg-indigo-50' : '' %>">
          <span class="text-lg">🇰🇷</span>
          <span>한국어</span>
          <% if I18n.locale == :ko %>
            <svg class="ml-auto w-4 h-4 text-indigo-600" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"></path>
            </svg>
          <% end %>
        </button>
      <% end %>
    </div>
  </div>
</div>
